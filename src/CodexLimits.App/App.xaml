<Application x:Class="CodexLimits.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown"
             Startup="Application_Startup">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#FF0B0C0E" />
        <SolidColorBrush x:Key="PanelBackground" Color="#FF111315" />
        <SolidColorBrush x:Key="BorderBrush" Color="#FF34373B" />
        <SolidColorBrush x:Key="PrimaryText" Color="#FFF4F4F5" />
        <SolidColorBrush x:Key="SecondaryText" Color="#FFA0A3A8" />

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
            <Setter Property="Background" Value="#FF202328" />
            <Setter Property="BorderBrush" Value="#FF454950" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
            <Setter Property="Background" Value="#FF1C1F23" />
            <Setter Property="BorderBrush" Value="#FF454950" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="White" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#FF101114" />
            <Setter Property="Background" Value="#FFF0F0F0" />
            <Setter Property="BorderBrush" Value="#FF555960" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,3" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
        </Style>
    </Application.Resources>
</Application>
